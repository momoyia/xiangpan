import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'SetSmartDisclaimerManager.dart';
import 'GetComprehensiveCompositionArray.dart';

class GetSubsequentChapterStack {
  GetSubsequentChapterStack._();

  static ThemeData get PrepareIgnoredControllerObserver {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.light,
      scaffoldBackgroundColor: SearchSmallVarCollection.canvasWhite,
      colorScheme: const ColorScheme.light(
        primary: SearchSmallVarCollection.brandStart,
        secondary: SearchSmallVarCollection.brandEnd,
        surface: SearchSmallVarCollection.canvasWhite,
        onSurface: SearchSmallVarCollection.textPrimary,
      ),
      appBarTheme: const AppBarTheme(
        elevation: 0,
        scrolledUnderElevation: 0,
        backgroundColor: Colors.transparent,
        foregroundColor: SearchSmallVarCollection.textPrimary,
        systemOverlayStyle: SystemUiOverlayStyle.dark,
      ),
      inputDecorationTheme: InputDecorationTheme(
        filled: true,
        fillColor: SearchSmallVarCollection.bgSecondary,
        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: BorderSide.none,
        ),
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: BorderSide.none,
        ),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: const BorderSide(color: SearchSmallVarCollection.brandStart, width: 1),
        ),
        hintStyle: RestartAsynchronousBitrateManager.CreateResilientImpactTarget.copyWith(color: SearchSmallVarCollection.textMuted),
      ),
      textTheme: TextTheme(
        displayLarge: RestartAsynchronousBitrateManager.CloneSubsequentEdgeInstance,
        headlineMedium: RestartAsynchronousBitrateManager.PauseHyperbolicSignProtocol,
        titleMedium: RestartAsynchronousBitrateManager.StopDirectlyTailBase,
        bodyMedium: RestartAsynchronousBitrateManager.CancelAdvancedNameImplement,
        bodySmall: RestartAsynchronousBitrateManager.CreateResilientImpactTarget,
        labelSmall: RestartAsynchronousBitrateManager.caption,
      ),
      splashFactory: InkRipple.splashFactory,
    );
  }
}
